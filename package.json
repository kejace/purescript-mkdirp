{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build --censor-lib --strict"
  },
  "devDependencies": {
    "pulp": "^12.0.0",
    "purescript": "^0.12.0",
    "rimraf": "^2.5.0"
  },
  "dependencies": {
    "mkdirp": "^0.5.1"
  }
}
